<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />
  </head>
  <body style="background-color: #b8bbd1">
    <div class="container">
      <h1 class="display-4 text-center py-1">YANGI REJA QOSHISH</h1>

      <div class="jumbotron p-3 shadow-sm">
        <form id="create-form" action="/create-item" method="POST">
          <div class="d-flex align-items-center">
            <input
              id="create-field"
              name="reja"
              autofocus
              autocomplete="off"
              class="form-control mr-3"
              type="text"
              style="flex: 1"
            />

            <ul id="item-list" class="list-group pb-5">
              <% itimes.map(function(item) { %>

            <li class="list-group-item list-group-item-info d-flex align-items-center">

            <span class="item-text"> <%= items.reja %> </span>
           <div>
            <button data-id="<%=item._id%>" 
              class="edit-me btn btn-secondary btn-sm mr-1">
                O'zgartirish
            </button>
            <button  data-id="<%=item._id%>"  class="delete-me btn btn-danger btn-sm">ochirish</button>
             </div>
            </li>
            <% }) %>
            </ul>
            <button class="btn btn-primary" style="border-radius: 20px">
              Yangi rejalarni ochirish
            </button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
